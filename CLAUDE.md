# Claude Agent Rules
Claude should always follow the same agent specifications as OpenCode.

Load the agent rules using: @~/.config/opencode/AGENTS.md
Skills are available in: ~/.config/opencode/skills/

These specifications define XP and Lean development practices for all agents.